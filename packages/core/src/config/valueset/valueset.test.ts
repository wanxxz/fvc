import { fieldset } from '../fieldset/fieldset.test'
import { type Valueset } from './valueset'

const valueset: Valueset = {
  id: '...',
  fieldsetId: fieldset.id,
  values: ['name', 0, false, ['male'], new Date(), {}]
}
